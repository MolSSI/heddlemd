# Your First Simulation

> **Stub.** Walk a new user through running the bundled example end-to-end.
>
> The `examples/lj-10000-argon/` bundle is a complete 10,000-atom
> Lennard-Jones argon system; 100 timesteps run in roughly a second on a
> recent NVIDIA GPU:
>
> ```
> ./target/release/dynamics run examples/lj-10000-argon/sim.toml
> ```
>
> Describe the three output files (`sim-traj.xyz`, `sim.log`, `sim.timings`)
> and what the user should see if it worked.
