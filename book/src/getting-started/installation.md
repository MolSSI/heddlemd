# Installation

> **Stub.** Fill in the platform-specific install details. The high-level
> prerequisites from the project README are:
>
> - An NVIDIA GPU with a recent driver.
> - CUDA Toolkit 11.8 or newer on `PATH` so `nvcc` can compile the device
>   kernels at build time.
> - Rust (Cargo edition 2024) — install via [rustup](https://rustup.rs/).
>
> Then:
>
> ```
> cargo build --release
> ```
>
> Produces the binary at `target/release/dynamics`.
